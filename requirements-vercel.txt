# Vercel-compatible requirements - minimal dependencies for serverless deployment
# Core Framework Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Image Processing (lightweight)
Pillow==10.1.0

# Async and Concurrency
aiofiles==23.2.1

# Networking and APIs
requests==2.31.0

# Configuration and Environment
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2

# Security (basic)
cryptography==41.0.8

# Note: Heavy ML dependencies (opencv, torch, scikit-learn) are excluded
# for Vercel deployment to reduce bundle size and improve cold start times
